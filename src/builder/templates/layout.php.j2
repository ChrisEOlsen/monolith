<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title | default('MyApp') }}</title>
    
    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    
    <!-- Tailwind CSS -->
    <?php if (getenv('APP_ENV') === 'prod'): ?>
        <link rel="stylesheet" href="/style.css">
    <?php else: ?>
        <script src="https://cdn.tailwindcss.com"></script>
    <?php endif; ?>
</head>
<body class="bg-gray-50 text-gray-900 font-sans">
    <nav class="bg-white border-b border-gray-200 px-6 py-4 mb-8">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-xl font-bold text-indigo-600">MyApp</a>
            <div class="space-x-4">
                <!-- Navigation links can be injected here -->
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-6">
        {{ content }}
    </main>

    <footer class="mt-12 py-6 text-center text-gray-500 text-sm">
        &copy; <?php echo date('Y'); ?> MyApp. Generated by AI.
    </footer>
</body>
</html>
