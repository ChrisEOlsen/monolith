# Disable directory listing
Options -Indexes

# Enable Rewrite Engine
RewriteEngine On

# Route everything to index.php if it's not a real file
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
