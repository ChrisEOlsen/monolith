# Project Identity
# Used for container names and network names to avoid collisions
APP_NAME=php-monolith

# External Ports
# Change these if you have multiple projects running
APP_PORT=1234
DB_PORT=3306
PMA_PORT=8080

# Database Configuration
DB_DATABASE=myapp
DB_USER=user
DB_PASSWORD=password
DB_ROOT_PASSWORD=root
